//// Wild circles #0.

// Setup:  screen size. //
void setup() {
  size(600, 400);                // Try changing the width & height.
}

// Next frame:  circle at (x,y). //
void draw() {
  ellipseMode(CENTER);
  fill(255,180,0);
  ellipse(300,200, 100,100 );       // Try changing the (x,y) and size.
}
